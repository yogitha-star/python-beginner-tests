{
  "name": "python-beginner-tests",
  "version": "1.0.0",
  "scripts": {
    "dev": "bun run server.ts",
    "build": "bun run build.ts",
    "start": "NODE_ENV=production bun run server.ts",
    "deploy": "bun run deploy.ts"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "bun-types": "latest"
  }
}
